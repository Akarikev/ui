---
title: Progress
description: "Displays an indicator showing the completion progress of a task."
---

<ComponentPreviewTabs component="progress" code="import { Progress } from '@/components/ui/progress'

export function ProgressDemo() {
  return (
    <Progress />
  )
}" radixCode="import { Progress } from '@/components/ui/progress'

export function ProgressDemo() {
  return (
    <Progress />
  )
}" />

## Installation

<InstallCommand command="elorm add progress" />

## Usage

<LibraryCodeBlock baseCode="import { Progress } from '@/components/ui/progress'" radixCode="import { Progress } from '@/components/ui/progress'" language="tsx" />

Displays an indicator showing the completion progress of a task.

## Examples

### Default

<ComponentPreviewTabs component="progress" code="import { Progress } from '@/components/ui/progress'

export function ProgressDemo() {
  return (
    <Progress />
  )
}" radixCode="import { Progress } from '@/components/ui/progress'

export function ProgressDemo() {
  return (
    <Progress />
  )
}" />
