---
title: Sheet
description: "Extends the Dialog component to display content that complements the main content."
---

<ComponentPreviewTabs component="sheet" code="import {
  Sheet,
  SheetContent,
  SheetHeader,
  SheetTitle,
  SheetTrigger,
} from '@/components/ui/sheet'

export function SheetDemo() {
  return (
    <Sheet>
      <SheetTrigger>Open</SheetTrigger>
      <SheetContent>
        <SheetHeader>
          <SheetTitle>Title</SheetTitle>
        </SheetHeader>
      </SheetContent>
    </Sheet>
  )
}" radixCode="import {
  Sheet,
  SheetContent,
  SheetHeader,
  SheetTitle,
  SheetTrigger,
} from '@/components/ui/sheet'

// Radix preset: generated via `elorm init --ui-library radix`

export function SheetDemo() {
  return (
    <Sheet>
      <SheetTrigger>Open</SheetTrigger>
      <SheetContent>
        <SheetHeader>
          <SheetTitle>Title</SheetTitle>
        </SheetHeader>
      </SheetContent>
    </Sheet>
  )
}" />

## Installation

<InstallCommand command="elorm add sheet" />

## Usage

<LibraryCodeBlock baseCode="import {
  Sheet,
  SheetContent,
  SheetHeader,
  SheetTitle,
  SheetTrigger,
} from '@/components/ui/sheet'" radixCode="import {
  Sheet,
  SheetContent,
  SheetHeader,
  SheetTitle,
  SheetTrigger,
} from '@/components/ui/sheet'

// Radix preset: generated via `elorm init --ui-library radix`" language="tsx" />

Extends the Dialog component to display content that complements the main content.

## Examples

### Default

<ComponentPreviewTabs component="sheet" code="import {
  Sheet,
  SheetContent,
  SheetHeader,
  SheetTitle,
  SheetTrigger,
} from '@/components/ui/sheet'

export function SheetDemo() {
  return (
    <Sheet>
      <SheetTrigger>Open</SheetTrigger>
      <SheetContent>
        <SheetHeader>
          <SheetTitle>Title</SheetTitle>
        </SheetHeader>
      </SheetContent>
    </Sheet>
  )
}" radixCode="import {
  Sheet,
  SheetContent,
  SheetHeader,
  SheetTitle,
  SheetTrigger,
} from '@/components/ui/sheet'

// Radix preset: generated via `elorm init --ui-library radix`

export function SheetDemo() {
  return (
    <Sheet>
      <SheetTrigger>Open</SheetTrigger>
      <SheetContent>
        <SheetHeader>
          <SheetTitle>Title</SheetTitle>
        </SheetHeader>
      </SheetContent>
    </Sheet>
  )
}" />
