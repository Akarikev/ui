---
title: Breadcrumb
description: "Displays the path to the current resource using a hierarchy of links."
---

<ComponentPreviewTabs component="breadcrumb" code="import { Breadcrumb } from '@/components/ui/breadcrumb'

export function BreadcrumbDemo() {
  return (
    <Breadcrumb />
  )
}" radixCode="import { Breadcrumb } from '@/components/ui/breadcrumb'

export function BreadcrumbDemo() {
  return (
    <Breadcrumb />
  )
}" />

## Installation

<InstallCommand command="elorm add breadcrumb" />

## Usage

<LibraryCodeBlock baseCode="import { Breadcrumb } from '@/components/ui/breadcrumb'" radixCode="import { Breadcrumb } from '@/components/ui/breadcrumb'" language="tsx" />

Displays the path to the current resource using a hierarchy of links.

## Examples

### Default

<ComponentPreviewTabs component="breadcrumb" code="import { Breadcrumb } from '@/components/ui/breadcrumb'

export function BreadcrumbDemo() {
  return (
    <Breadcrumb />
  )
}" radixCode="import { Breadcrumb } from '@/components/ui/breadcrumb'

export function BreadcrumbDemo() {
  return (
    <Breadcrumb />
  )
}" />
