---
title: Stat Card
description: "A dashboard stat card with optional trend indicator."
---

<ComponentPreviewTabs component="stat-card" code="import { StatCard } from '@/components/blocks/stat-card'

export function StatCardDemo() {
  return (
    <StatCard title=&quot;Revenue&quot; value=&quot;$12,420&quot; description=&quot;+12% from last month&quot; trend={{ value: &quot;+12%&quot;, positive: true }} />
  )
}" radixCode="import { StatCard } from '@/components/blocks/stat-card'

export function StatCardDemo() {
  return (
    <StatCard title=&quot;Revenue&quot; value=&quot;$12,420&quot; description=&quot;+12% from last month&quot; trend={{ value: &quot;+12%&quot;, positive: true }} />
  )
}" />

## Installation

<InstallCommand command="elorm add stat-card" />

## Usage

<LibraryCodeBlock baseCode="import { StatCard } from '@/components/blocks/stat-card'" radixCode="import { StatCard } from '@/components/blocks/stat-card'" language="tsx" />

A dashboard stat card with optional trend indicator.

## Examples

### Default

<ComponentPreviewTabs component="stat-card" code="import { StatCard } from '@/components/blocks/stat-card'

export function StatCardDemo() {
  return (
    <StatCard title=&quot;Revenue&quot; value=&quot;$12,420&quot; description=&quot;+12% from last month&quot; trend={{ value: &quot;+12%&quot;, positive: true }} />
  )
}" radixCode="import { StatCard } from '@/components/blocks/stat-card'

export function StatCardDemo() {
  return (
    <StatCard title=&quot;Revenue&quot; value=&quot;$12,420&quot; description=&quot;+12% from last month&quot; trend={{ value: &quot;+12%&quot;, positive: true }} />
  )
}" />
