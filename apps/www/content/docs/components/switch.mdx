---
title: Switch
description: "A control that allows the user to toggle between checked and not checked."
---

<ComponentPreviewTabs component="switch" code="import { Switch } from '@/components/ui/switch'

export function SwitchDemo() {
  return (
    <Switch />
  )
}" radixCode="import { Switch } from '@/components/ui/switch'

export function SwitchDemo() {
  return (
    <Switch />
  )
}" />

## Installation

<InstallCommand command="elorm add switch" />

## Usage

<LibraryCodeBlock baseCode="import { Switch } from '@/components/ui/switch'" radixCode="import { Switch } from '@/components/ui/switch'" language="tsx" />

A control that allows the user to toggle between checked and not checked.

## Examples

### Default

<ComponentPreviewTabs component="switch" code="import { Switch } from '@/components/ui/switch'

export function SwitchDemo() {
  return (
    <Switch />
  )
}" radixCode="import { Switch } from '@/components/ui/switch'

export function SwitchDemo() {
  return (
    <Switch />
  )
}" />
