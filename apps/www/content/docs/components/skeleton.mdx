---
title: Skeleton
description: "Use to show a placeholder while content is loading."
---

<ComponentPreviewTabs component="skeleton" code="import { Skeleton } from '@/components/ui/skeleton'

export function SkeletonDemo() {
  return (
    <Skeleton className=&quot;h-4 w-full&quot; />
  )
}" radixCode="import { Skeleton } from '@/components/ui/skeleton'

export function SkeletonDemo() {
  return (
    <Skeleton className=&quot;h-4 w-full&quot; />
  )
}" />

## Installation

<InstallCommand command="elorm add skeleton" />

## Usage

<LibraryCodeBlock baseCode="import { Skeleton } from '@/components/ui/skeleton'" radixCode="import { Skeleton } from '@/components/ui/skeleton'" language="tsx" />

Use to show a placeholder while content is loading.

## Examples

### Default

<ComponentPreviewTabs component="skeleton" code="import { Skeleton } from '@/components/ui/skeleton'

export function SkeletonDemo() {
  return (
    <Skeleton className=&quot;h-4 w-full&quot; />
  )
}" radixCode="import { Skeleton } from '@/components/ui/skeleton'

export function SkeletonDemo() {
  return (
    <Skeleton className=&quot;h-4 w-full&quot; />
  )
}" />
