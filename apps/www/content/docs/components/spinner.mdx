---
title: Spinner
description: "Loading indicator for buttons and async states."
---

<ComponentPreviewTabs component="spinner" code="import { Spinner } from '@/components/ui/spinner'

export function SpinnerDemo() {
  return (
    <Spinner />
  )
}" radixCode="import { Spinner } from '@/components/ui/spinner'

export function SpinnerDemo() {
  return (
    <Spinner />
  )
}" />

## Installation

<InstallCommand command="elorm add spinner" />

## Usage

<LibraryCodeBlock baseCode="import { Spinner } from '@/components/ui/spinner'" radixCode="import { Spinner } from '@/components/ui/spinner'" language="tsx" />

Loading indicator for buttons and async states.

## Examples

### Default

<ComponentPreviewTabs component="spinner" code="import { Spinner } from '@/components/ui/spinner'

export function SpinnerDemo() {
  return (
    <Spinner />
  )
}" radixCode="import { Spinner } from '@/components/ui/spinner'

export function SpinnerDemo() {
  return (
    <Spinner />
  )
}" />
