---
title: Popover
description: "Displays rich content in a portal, triggered by a button."
---

<ComponentPreviewTabs component="popover" code="import { Popover } from '@/components/ui/popover'

export function PopoverDemo() {
  return (
    <Popover />
  )
}" radixCode="import { Popover } from '@/components/ui/popover'

export function PopoverDemo() {
  return (
    <Popover />
  )
}" />

## Installation

<InstallCommand command="elorm add popover" />

## Usage

<LibraryCodeBlock baseCode="import { Popover } from '@/components/ui/popover'" radixCode="import { Popover } from '@/components/ui/popover'" language="tsx" />

Displays rich content in a portal, triggered by a button.

## Examples

### Default

<ComponentPreviewTabs component="popover" code="import { Popover } from '@/components/ui/popover'

export function PopoverDemo() {
  return (
    <Popover />
  )
}" radixCode="import { Popover } from '@/components/ui/popover'

export function PopoverDemo() {
  return (
    <Popover />
  )
}" />
