---
title: Tabs
description: "A set of layered sections of content displayed one at a time."
---

<ComponentPreviewTabs component="tabs" code="import { Tabs } from '@/components/ui/tabs'

export function TabsDemo() {
  return (
    <Tabs />
  )
}" radixCode="import { Tabs } from '@/components/ui/tabs'

export function TabsDemo() {
  return (
    <Tabs />
  )
}" />

## Installation

<InstallCommand command="elorm add tabs" />

## Usage

<LibraryCodeBlock baseCode="import { Tabs } from '@/components/ui/tabs'" radixCode="import { Tabs } from '@/components/ui/tabs'" language="tsx" />

A set of layered sections of content displayed one at a time.

## Examples

### Default

<ComponentPreviewTabs component="tabs" code="import { Tabs } from '@/components/ui/tabs'

export function TabsDemo() {
  return (
    <Tabs />
  )
}" radixCode="import { Tabs } from '@/components/ui/tabs'

export function TabsDemo() {
  return (
    <Tabs />
  )
}" />
