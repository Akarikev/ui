---
title: Alert
description: "Displays a callout for user attention."
---

<ComponentPreviewTabs component="alert" code="import { Alert } from '@/components/ui/alert'

export function AlertDemo() {
  return (
    <Alert />
  )
}" radixCode="import { Alert } from '@/components/ui/alert'

export function AlertDemo() {
  return (
    <Alert />
  )
}" />

## Installation

<InstallCommand command="elorm add alert" />

## Usage

<LibraryCodeBlock baseCode="import { Alert } from '@/components/ui/alert'" radixCode="import { Alert } from '@/components/ui/alert'" language="tsx" />

Displays a callout for user attention.

## Examples

### Default

<ComponentPreviewTabs component="alert" code="import { Alert } from '@/components/ui/alert'

export function AlertDemo() {
  return (
    <Alert />
  )
}" radixCode="import { Alert } from '@/components/ui/alert'

export function AlertDemo() {
  return (
    <Alert />
  )
}" />
