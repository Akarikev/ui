---
title: Toggle Group
description: "A set of two-state buttons that can be toggled on or off."
---

<ComponentPreviewTabs component="toggle-group" code="import { ToggleGroup } from '@/components/ui/toggle-group'

export function ToggleGroupDemo() {
  return (
    <ToggleGroup />
  )
}" radixCode="import { ToggleGroup } from '@/components/ui/toggle-group'

export function ToggleGroupDemo() {
  return (
    <ToggleGroup />
  )
}" />

## Installation

<InstallCommand command="elorm add toggle-group" />

## Usage

<LibraryCodeBlock baseCode="import { ToggleGroup } from '@/components/ui/toggle-group'" radixCode="import { ToggleGroup } from '@/components/ui/toggle-group'" language="tsx" />

A set of two-state buttons that can be toggled on or off.

## Examples

### Default

<ComponentPreviewTabs component="toggle-group" code="import { ToggleGroup } from '@/components/ui/toggle-group'

export function ToggleGroupDemo() {
  return (
    <ToggleGroup />
  )
}" radixCode="import { ToggleGroup } from '@/components/ui/toggle-group'

export function ToggleGroupDemo() {
  return (
    <ToggleGroup />
  )
}" />
