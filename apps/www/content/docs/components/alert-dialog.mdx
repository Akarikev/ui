---
title: Alert Dialog
description: "A modal dialog that interrupts the user with important content."
---

<ComponentPreviewTabs component="alert-dialog" code="import { AlertDialog } from '@/components/ui/alert-dialog'

export function AlertDialogDemo() {
  return (
    <AlertDialog />
  )
}" radixCode="import { AlertDialog } from '@/components/ui/alert-dialog'

export function AlertDialogDemo() {
  return (
    <AlertDialog />
  )
}" />

## Installation

<InstallCommand command="elorm add alert-dialog" />

## Usage

<LibraryCodeBlock baseCode="import { AlertDialog } from '@/components/ui/alert-dialog'" radixCode="import { AlertDialog } from '@/components/ui/alert-dialog'" language="tsx" />

A modal dialog that interrupts the user with important content.

## Examples

### Default

<ComponentPreviewTabs component="alert-dialog" code="import { AlertDialog } from '@/components/ui/alert-dialog'

export function AlertDialogDemo() {
  return (
    <AlertDialog />
  )
}" radixCode="import { AlertDialog } from '@/components/ui/alert-dialog'

export function AlertDialogDemo() {
  return (
    <AlertDialog />
  )
}" />
