---
title: Login Form
description: "A sign-in form block with email and password fields."
---

<ComponentPreviewTabs component="login-form" code="import { LoginForm } from '@/components/blocks/login-form'

export function LoginFormDemo() {
  return (
    <LoginForm />
  )
}" radixCode="import { LoginForm } from '@/components/blocks/login-form'

export function LoginFormDemo() {
  return (
    <LoginForm />
  )
}" />

## Installation

<InstallCommand command="elorm add login-form" />

## Usage

<LibraryCodeBlock baseCode="import { LoginForm } from '@/components/blocks/login-form'" radixCode="import { LoginForm } from '@/components/blocks/login-form'" language="tsx" />

A sign-in form block with email and password fields.

## Examples

### Default

<ComponentPreviewTabs component="login-form" code="import { LoginForm } from '@/components/blocks/login-form'

export function LoginFormDemo() {
  return (
    <LoginForm />
  )
}" radixCode="import { LoginForm } from '@/components/blocks/login-form'

export function LoginFormDemo() {
  return (
    <LoginForm />
  )
}" />
