---
title: Tooltip
description: "A popup that displays information related to an element."
---

<ComponentPreviewTabs component="tooltip" code="import {
  Tooltip,
  TooltipContent,
  TooltipTrigger,
} from '@/components/ui/tooltip'

export function TooltipDemo() {
  return (
    <Tooltip>
      <TooltipTrigger>Hover me</TooltipTrigger>
      <TooltipContent>Helpful tip</TooltipContent>
    </Tooltip>
  )
}" radixCode="import {
  Tooltip,
  TooltipContent,
  TooltipTrigger,
} from '@/components/ui/tooltip'

export function TooltipDemo() {
  return (
    <Tooltip>
      <TooltipTrigger>Hover me</TooltipTrigger>
      <TooltipContent>Helpful tip</TooltipContent>
    </Tooltip>
  )
}" />

## Installation

<InstallCommand command="elorm add tooltip" />

## Usage

<LibraryCodeBlock baseCode="import {
  Tooltip,
  TooltipContent,
  TooltipTrigger,
} from '@/components/ui/tooltip'" radixCode="import {
  Tooltip,
  TooltipContent,
  TooltipTrigger,
} from '@/components/ui/tooltip'" language="tsx" />

A popup that displays information related to an element.

Wrap app in TooltipProvider. Keep tooltip text concise.

## Examples

### Default

<ComponentPreviewTabs component="tooltip" code="import {
  Tooltip,
  TooltipContent,
  TooltipTrigger,
} from '@/components/ui/tooltip'

export function TooltipDemo() {
  return (
    <Tooltip>
      <TooltipTrigger>Hover me</TooltipTrigger>
      <TooltipContent>Helpful tip</TooltipContent>
    </Tooltip>
  )
}" radixCode="import {
  Tooltip,
  TooltipContent,
  TooltipTrigger,
} from '@/components/ui/tooltip'

export function TooltipDemo() {
  return (
    <Tooltip>
      <TooltipTrigger>Hover me</TooltipTrigger>
      <TooltipContent>Helpful tip</TooltipContent>
    </Tooltip>
  )
}" />
