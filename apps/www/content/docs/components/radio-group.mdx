---
title: Radio Group
description: "A set of checkable buttons where only one can be checked."
---

<ComponentPreviewTabs component="radio-group" code="import { RadioGroup } from '@/components/ui/radio-group'

export function RadioGroupDemo() {
  return (
    <RadioGroup />
  )
}" radixCode="import { RadioGroup } from '@/components/ui/radio-group'

export function RadioGroupDemo() {
  return (
    <RadioGroup />
  )
}" />

## Installation

<InstallCommand command="elorm add radio-group" />

## Usage

<LibraryCodeBlock baseCode="import { RadioGroup } from '@/components/ui/radio-group'" radixCode="import { RadioGroup } from '@/components/ui/radio-group'" language="tsx" />

A set of checkable buttons where only one can be checked.

## Examples

### Default

<ComponentPreviewTabs component="radio-group" code="import { RadioGroup } from '@/components/ui/radio-group'

export function RadioGroupDemo() {
  return (
    <RadioGroup />
  )
}" radixCode="import { RadioGroup } from '@/components/ui/radio-group'

export function RadioGroupDemo() {
  return (
    <RadioGroup />
  )
}" />
