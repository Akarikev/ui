---
title: Badge
description: "Displays a badge or a component that looks like a badge."
---

<ComponentPreviewTabs component="badge" code="import { Badge } from '@/components/ui/badge'

export function BadgeDemo() {
  return (
    <Badge>New</Badge>
  )
}" radixCode="import { Badge } from '@/components/ui/badge'

export function BadgeDemo() {
  return (
    <Badge>New</Badge>
  )
}" />

## Installation

<InstallCommand command="elorm add badge" />

## Usage

<LibraryCodeBlock baseCode="import { Badge } from '@/components/ui/badge'" radixCode="import { Badge } from '@/components/ui/badge'" language="tsx" />

Displays a badge or a component that looks like a badge.

## Examples

### Default

<ComponentPreviewTabs component="badge" code="import { Badge } from '@/components/ui/badge'

export function BadgeDemo() {
  return (
    <Badge>New</Badge>
  )
}" radixCode="import { Badge } from '@/components/ui/badge'

export function BadgeDemo() {
  return (
    <Badge>New</Badge>
  )
}" />
