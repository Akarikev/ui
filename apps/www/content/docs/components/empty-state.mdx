---
title: Empty State
description: "A block for displaying empty or no-results states."
---

<ComponentPreviewTabs component="empty-state" code="import { EmptyState } from '@/components/blocks/empty-state'

export function EmptyStateDemo() {
  return (
    <EmptyState title=&quot;No results found&quot; description=&quot;Try adjusting your search.&quot; />
  )
}" radixCode="import { EmptyState } from '@/components/blocks/empty-state'

export function EmptyStateDemo() {
  return (
    <EmptyState title=&quot;No results found&quot; description=&quot;Try adjusting your search.&quot; />
  )
}" />

## Installation

<InstallCommand command="elorm add empty-state" />

## Usage

<LibraryCodeBlock baseCode="import { EmptyState } from '@/components/blocks/empty-state'" radixCode="import { EmptyState } from '@/components/blocks/empty-state'" language="tsx" />

A block for displaying empty or no-results states.

Use when lists, tables, or search results are empty. Provide a clear action when possible.

## Examples

### Default

<ComponentPreviewTabs component="empty-state" code="import { EmptyState } from '@/components/blocks/empty-state'

export function EmptyStateDemo() {
  return (
    <EmptyState title=&quot;No results found&quot; description=&quot;Try adjusting your search.&quot; />
  )
}" radixCode="import { EmptyState } from '@/components/blocks/empty-state'

export function EmptyStateDemo() {
  return (
    <EmptyState title=&quot;No results found&quot; description=&quot;Try adjusting your search.&quot; />
  )
}" />
