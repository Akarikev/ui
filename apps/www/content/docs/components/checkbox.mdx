---
title: Checkbox
description: "A control that allows the user to toggle between checked and not checked."
---

<ComponentPreviewTabs component="checkbox" code="import { Checkbox } from '@/components/ui/checkbox'

export function CheckboxDemo() {
  return (
    <Checkbox />
  )
}" radixCode="import { Checkbox } from '@/components/ui/checkbox'

export function CheckboxDemo() {
  return (
    <Checkbox />
  )
}" />

## Installation

<InstallCommand command="elorm add checkbox" />

## Usage

<LibraryCodeBlock baseCode="import { Checkbox } from '@/components/ui/checkbox'" radixCode="import { Checkbox } from '@/components/ui/checkbox'" language="tsx" />

A control that allows the user to toggle between checked and not checked.

## Examples

### Default

<ComponentPreviewTabs component="checkbox" code="import { Checkbox } from '@/components/ui/checkbox'

export function CheckboxDemo() {
  return (
    <Checkbox />
  )
}" radixCode="import { Checkbox } from '@/components/ui/checkbox'

export function CheckboxDemo() {
  return (
    <Checkbox />
  )
}" />
